import { render } from '@testing-library/react';
import { ArticleDetails } from './ArticleDetails';


describe('ArticleDetails', () => {
  test('Simple ArticleDetails Test', () => {
    render(<ArticleDetails/>);
    expect())
  });
});
