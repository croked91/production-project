import { render } from '@testing-library/react';
import { Skeleton } from './Skeleton';


describe('Skeleton', () => {
  test('Simple Skeleton Test', () => {
    render(<Skeleton/>);
    expect())
  });
});
