import { render } from '@testing-library/react';
import ArticlesPage from './ArticlesPage';

describe('ArticlesPage', () => {
  test('Simple ArticlesPage Test', () => {
    render(<ArticlesPage />);
  });
});
