import {<FTName>} from './<FTName>';
import { render, screen } from '@testing-library/react';


describe('<FTName>', () => {
  test('Simple <FTName> Test', () => {
    render(<<FTName>/>);
    expect())
  });
});
